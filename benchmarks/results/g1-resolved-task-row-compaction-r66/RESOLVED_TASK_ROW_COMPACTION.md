# Bonesaw resolved task-row compaction · r66

## Decision · PROMOTE

Production now removes exact-zero rows at any priority and satisfied fixed-bound rows only from terminal Style before dense pseudoinverses. The retained four-step corpus avoids 20,176 inert row-instances. Five alternating pinned control/candidate pairs reduce whole-process retired instructions by 3.861% with every pair lower. The production default passes 43/43 admission gates, r63 passes 8/8, and the independent Pinocchio/NumPy oracle passes 6/6.

> Fixed-capacity task declarations and diagnostics do not change. This is internal dense solve compaction, not deletion of an authority layer. Terminal fixed rows are constant over the feasible set and Style has no lower consumer; higher-priority clipping provenance remains untouched.

## Pinned five-pair process evidence

| variant | instructions B | cycles B | task-clock s | CPU s | p50 / p99 / max µs | RSS MB |
|---|---|---|---|---|---|---|
| control | 239.795126 | 62.645016 | 15.055975 | 7.327 | 3347.4 / 5626.3 / 7543.1 | 50.28 |
| candidate | 230.536490 | 60.354694 | 14.482483 | 7.062 | 3337.7 / 5632.9 / 7547.2 | 50.31 |

The counter boundary is the complete pinned Python admission process, including morphology, two Rust corpus passes, reporting, and serialization. It is not isolated per-solve attribution. Host-sensitive p99 is flat within noise; stable instruction work decides promotion.

## Ordinary WBC numerical boundary

| signal | changed ticks | RMS delta | maximum delta | contract |
|---|---|---|---|---|
| generalized acceleration | 793 | 9.577e-10 | 1.502e-07 | ≤ 2.0e-06 |
| actuator effort | 793 | 2.472e-10 | 1.459e-08 | ≤ 2.0e-07 |
| normal force | 791 | 9.678e-09 | 5.695e-07 | ≤ 2.0e-06 |
| task RMS | 793 | 2.003e-10 | 3.231e-08 | ≤ 5.0e-07 |
| Jacobi sweeps | 169 | 2.725e-01 | 2.000e+00 | ≤ 2.0e+00 |

All double-support physical arrays remain bit-exact. Changes occur only within the 916 single-support ticks where twelve unused contact-force slots are hard-fixed to zero. Status, task clipping, pseudoinverse counts, active-set steps, feasibility work, and allocation arrays remain exact. Jacobi sweep count changes by at most two; every physical delta remains at least an order of magnitude inside its declared contract.

## Downstream consequence and independent oracle

All six r63 generalized-acceleration consequence traces are preserved within 5.421e-20; fixed actuator effort and bound-normalized pressure are bit-exact. Redundant contact-force distribution changes by at most 2.757e-06 N. The r63 allocation/equality/repeat gates pass 8/8.

Pinocchio 4.0 independently reconstructs dynamics/contact products and NumPy independently reconstructs the four-level hierarchy at 48 edge/even/adverse states. All r64 gates pass 6/6 under the production default.

## Gates

- PASS `source_artifacts_remain_immutable`
- PASS `candidate_reduces_median_whole_process_instructions_by_at_least_3pct`
- PASS `candidate_reduces_instructions_in_every_pinned_pair`
- PASS `production_default_passes_all_43_admission_gates`
- PASS `ordinary_changed_fields_stay_inside_declared_delta_contract`
- PASS `status_clipping_work_and_allocation_decisions_are_exact`
- PASS `all_double_support_physical_arrays_are_bit_exact`
- PASS `r63_consequence_passes_all_8_mechanism_gates`
- PASS `r63_acceleration_effort_and_pressure_are_preserved`
- PASS `r64_independent_pinocchio_numpy_oracle_passes_6_of_6`
